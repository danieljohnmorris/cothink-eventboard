<% if (params[:topic]) %>
<p><strong><%= @events.length %></strong> upcoming events for topic <strong><%= params[:topic] %></strong> (<%= link_to "Show all events", root_path %>)</p>
<% end %>

<%= render :partial => 'events/events_list', :locals => { :events => @events } %>
